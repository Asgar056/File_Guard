# FileGuard v4

**FileGuard** is a reliable file integrity monitoring tool with both CLI and GUI interfaces.  
It detects new, modified, and deleted files in any directory on your system and logs alerts.

---

## Features

- Scan any folder on your system
- Detects **new, modified, and deleted files**
- Create a baseline for comparison
- Manual scan or **automatic periodic scan** (Watch Mode)
- Logs alerts to **console, GUI, and alerts.log**
- Optional **Tkinter GUI** with colored, scrollable alerts

---

## Installation

1. Clone the repository:

```bash
git clone https://github.com/YourUsername/FileGuard.git
cd FileGuard
